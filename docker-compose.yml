# version: "3"

services:
  app:
    container_name: node
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    stdin_open: true
    ports:
      - 3001:3001
    volumes:
      - type: bind
        source: "./"
        target: "/app"
